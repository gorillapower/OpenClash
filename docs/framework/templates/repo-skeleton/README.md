# Project Name

## Operating Model

This repo uses AI-Compatible Development Rhythm.

- `docs/decision/` holds durable product and contract decisions.
- `docs/architecture/` holds intended implementation structure and migration
  shape.
- `docs/execution-plan.md` holds epic order and near-term sequencing.
- `.github/ISSUE_TEMPLATE/ai_task.md` defines bounded AI execution units.

If implementation learning would change how future tasks should be written,
refresh the relevant docs before continuing.

## Recommended Variant

Start with the `Standard` variant unless this repo is small enough that the
extra structure would be mostly empty.
