# Repo Skeleton

This folder contains the committed framework files generated into a target repo
when AI-Compatible Development Rhythm is adopted.

Use this skeleton as the starting point, then tailor:

- tracker type
- hierarchy labels
- work classes
- verification expectations
- patch-lane behavior

`.ai-compatible-rhythm.yaml` is the thin machine-readable runtime profile for
the skill and future automation.
