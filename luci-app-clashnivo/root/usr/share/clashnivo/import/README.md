# Import Helper Area

This directory is reserved for built-in one-time import tooling and transformation helpers.

No runtime import implementation lives here yet. The directory exists so later import work has a stable home instead of reusing the flat top-level helper namespace.
